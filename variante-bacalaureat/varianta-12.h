#include "functii-varianta-12.h"

// Subiectul 3

// Problema 3

void rezolvareSub3Problema3() {
	int x[200], y[200], m, n, max, c = 0;
	citireProblema3(x, y, m, n);
	/*if (m > n) {
		max = m;
	}
	else {
		max = n;
	}

	int i = 0, j = 0, nr = 0, c = 0;
	while (i < max || j < max) {
		
	}*/

	// ? ? ? ?
}

// Problema 4

void rezolvareSub3Problema4() {
	int n, c;
	cout << "Introduceti n : ";
	cin >> n;
	cout << "Introducti c : ";
	cin >> c;

	cout << countAparitii(n, c);
}